// place files you want to import through the `$lib` alias in this folder.
export interface Question {
	text: string;
	A: string;
	B: string;
	C: string;
	D: string;
	correct: string;
	difficulty: number;
}
